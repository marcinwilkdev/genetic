use genetyk::benchmarking;

fn main() {
    // benchmarking::all_files_symmetric_vs_asymmetric();
    // benchmarking::all_files_population_size_dependence();
    benchmarking::all_files_memetic_fraction_dependence();
}
